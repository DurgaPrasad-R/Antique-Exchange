<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wishlist</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,400;12..96,800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/style.css">
    <style>
        /* Style for the table */
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th,td {
            text-align: center;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        /* Style for the images */
        .image img {
            max-width: 100px;
            max-height: 100px;
        }

        .dash{
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007BFF;
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .dash:hover{
            background-color: #0056b3;
        }

        .dboard{
            display: flex;
            justify-content: center;
        }

    </style>
</head>
<body>
    <%- include('header',{globalCategories:globalCategories}); -%>
    <% if (wishlist && wishlist.length > 0) { %>
    <h1>Your Wishlist</h1>
    <table>
        <thead>
            <tr>
                <th>Item Image</th>
                <th>Item Name</th>
                <th>Item Price</th>
                <th>View More</th>
            </tr>
        </thead>
        <tbody>
            <% wishlist.forEach((item) => { %>
                <tr>
                    <td class="image"><img src="<%= item.SellerItemImage %>" alt="<%= item.SellerItemName %>" width="100"></td>
                    <td><%= item.SellerItemName %></td>
                    <td>₹<%= item.SellerItemPrice %></td>
                    <td><a href="/item/<%= item.SellerCategory %>/<%= item.SellerItemName %>">View More</a></td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <% } else { %>
        <p>Your wishlist is empty.</p>
    <% } %>
    <div class="dboard">
        <a href="/dashboard" class="dash">Back to Dashboard</a>
    </div>
</body>
</html>
